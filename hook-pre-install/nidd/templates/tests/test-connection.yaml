apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nidd.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "nidd.name" . }}
    helm.sh/chart: {{ include "nidd.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nidd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
