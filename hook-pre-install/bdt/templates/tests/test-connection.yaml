apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bdt.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "bdt.name" . }}
    helm.sh/chart: {{ include "bdt.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bdt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
