apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monte.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "monte.name" . }}
    helm.sh/chart: {{ include "monte.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "monte.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
